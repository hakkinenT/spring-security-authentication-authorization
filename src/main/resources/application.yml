spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/auth_jwt
        username: postgres
        password: Th@l1891
        driver-class-name: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: create-drop
        show-sql: true
        database: postgresql
        database-plataform: org.hibernate.dialect.PostgreSQLDialect